[project]
name = "topcon-checker"
version = "0.1.0"
description = "Look up patients and extract images from copied TOPCON slit-lamp data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.2",
]
